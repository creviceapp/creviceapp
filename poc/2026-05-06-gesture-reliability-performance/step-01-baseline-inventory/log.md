# Log: Step 01 - Baseline Inventory

- 2026-05-06: Supervisor created the mission and delegated baseline inventory preparation.
- 2026-05-06: Read `mission.md`; confirmed Step 01 is an inventory-only pass with no benchmarking, commits, pushes, or `docs/` edits. Initial `git status --short --branch` showed branch `split-store-msix-release-zip...origin/split-store-msix-release-zip`, modified `CreviceAppTests/CreviceAppTests.csproj`, untracked `CreviceAppTests/GestureCoverageHarnessTests.cs`, untracked `docs/`, and untracked `poc/`.
- 2026-05-06: Inventoried solution/test configuration. `Crevice.sln` includes `CreviceLib`, `CreviceLibTests`, `CreviceApp`, `CreviceAppPackage`, and `CreviceAppTests`. Tests are MSTest .NET Framework 4.6.2 projects. CI restores legacy `packages.config` packages, builds `CreviceLib`, `CreviceLibTests`, and `CreviceAppTests`, but only runs `CreviceLibTests` with `vstest.console.exe`; app tests build but are not run in CI. The current app-test csproj change adds `GestureCoverageHarnessTests.cs`, which contains deterministic enumeration/replay helpers and a timing assertion named `PerfHarnessMeasuresGeneratedMotionReplay`.
- 2026-05-06: Traced the gesture path. `MouseGestureForm` translates low-level hook events to `GestureMachine.Input(...)`: mouse move uses `NullEvent` plus point, button events use physical press/release events plus point, vertical wheel maps negative delta to `WheelDown` and non-negative to `WheelUp`, horizontal wheel maps negative delta to `WheelRight` and non-negative to `WheelLeft`. App `GestureMachine.Input` stores cursor position, filters keyboard timeout keys, then delegates to core FSM input.
- 2026-05-06: Core FSM behavior found: `State0` activates matching `When` elements for single-fire or double-throw press triggers; `StateN` handles nested fire/press/release triggers and evaluates `StrokeWatcher.GetStrokeSequence()` on the normal release trigger. `StrokeWatcher` is asynchronous through a task factory and blocking queue; app `GestureMachine` uses a high-priority `LowLatencyScheduler`, while tests can set `StrokeWatchInterval = 0` or substitute a watcher.
- 2026-05-06: Default script loading flow found: `ReloadableGestureMachine` calls `GlobalConfig.GetOrSetDefaultUserScriptFile(Encoding.UTF8.GetString(Properties.Resources.DefaultUserScript))`, constructs `GestureMachineCandidate`, restores cache when allowed, otherwise parses/compiles/emits/evaluates the script, creates profiles, runs each profile's `GestureMachine`, and optionally wires gesture stroke overlay callbacks. `GlobalConfig` disables IDE support `#load` directives by default for runtime/test loading.
- 2026-05-06: Reusable helpers identified. Core deterministic tests can reuse `CreviceLibTests/TestEnv.cs` (`TestGestureMachine`, `TestCallbackManager`, `TestEvents`) and existing DSL/FSM/stroke tests. App-side deterministic script tests can reuse `CreviceAppTests/TestHelpers.SetupUserDirectory`, `ScriptsTests.DefaultUserScriptTest`, `GestureMachineCandidateTests`, and the untracked `GestureCoverageHarnessTests` enumeration/replay helpers. Machine-dependent boundaries are hook installation, SendInput, foreground/pointed window evaluation, cursor position, display scaling, timers, and any benchmark/timing assertions.
- 2026-05-06: Tooling check found no `msbuild`, `nuget`, or `vstest.console` on PATH. `.NET SDK 10.0.203` is installed, the .NET Framework 4.6.2 targeting pack exists, and required legacy packages exist under `packages/`. Visual Studio is installed at `C:\Program Files\Microsoft Visual Studio\18\Community`, with `vstest.console.exe` at `Common7\IDE\CommonExtensions\Microsoft\TestWindow\vstest.console.exe`.
- 2026-05-06: Build/test commands run. `dotnet msbuild CreviceLib\CreviceLib.csproj /p:Configuration=Release /verbosity:minimal` succeeded. `dotnet msbuild CreviceLibTests\CreviceLibTests.csproj /p:Configuration=Release /verbosity:minimal` succeeded. `dotnet msbuild CreviceAppTests\CreviceAppTests.csproj /p:Configuration=Release /verbosity:minimal` failed because `CreviceApp` could not resolve `System.Runtime.WindowsRuntime` and `System.Runtime.WindowsRuntime.UI.Xaml`, causing `IAsyncOperation<StartupTask>.AsTask/GetAwaiter` compile errors; CI's legacy Windows SDK contract installation is needed for that path. A first `dotnet test` attempt exited 0 but produced no TRX, so it is not treated as a result. Direct `vstest.console.exe` against `CreviceLibTests\bin\Release\CreviceLibTests.dll` with `/TestCaseFilter:"FullyQualifiedName!~EventsBenchmarkTest"` succeeded: 157 total, 157 passed, TRX saved under `output/test-results/CreviceLibTests.Step01.trx`.
- 2026-05-06: Wrote `report.md` with Step 02 recommendations and saved structured findings to `output/baseline-inventory.json`.
